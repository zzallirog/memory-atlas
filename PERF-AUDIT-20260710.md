# PERF-AUDIT 2026-07-10 — «60-120 fps независимо ни от чего»

Ультра-аудит рендер-пайплайна memory-atlas (Arch, вульт 1341 нот + 126 ghost / 6566 рёбер).
Стенд: headless Chromium (Playwright), тайминг draw-колбэка через rAF-обёртку
(`addInitScript`), объёмы канвас-вызовов через prototype-патч, K читается
`d3.zoomTransform(canvas).k`. Хедлес НЕ показывает растровую цену (Skia дефер),
поэтому итог = JS-тайминг + объёмы вызовов; растровые победы видны только на живой машине.

## Сводка «было → стало» (draw-колбэк, пан, без выделения)

| K (зум) | v2.10.1 | v2.10.4 | что в этой зоне |
|---------|---------|---------|-----------------|
| ~1.0    | ~4.4ms  | 4.0ms   | точки + лейблы |
| 1.58    | **46.4ms** | 6.3ms | порог карточек TILE_K=1.5 |
| 2.33    | **48.7ms** | 8.8ms | карточки + desc (DESC_K=2.1) |
| 3.25    | **48.5ms** | 8.4ms | BODY_K=3.2, 4 строки |
| 4.29    | **48.6ms** | 8.5ms | глубокое чтение |

Ego-блюр (выделение) — отдельная история, починена в v2.10.2: transform убран из
bg-штампа, блюр теперь delta-blit кэша (полный re-raster ≤1/150мс + settle).

## Найденные болезни (все — пер-кадровый пересчёт статики)

1. **bodySnip без кэша** — 4 regex по ПОЛНОМУ телу ноты (до ~40кБ) на узел на кадр
   внутри cardText→tileMetrics при K≥TILE_K. Дал обрыв 4.4→46мс ровно на пороге
   карточек («дески тянут одеяло»). Фикс: кэш по (node, cap). `2555568`
2. **measureText-шторм** — ~8 200 вызовов/кадр: fitLabel посимвольно + wrapText
   пословно, каждый кадр заново. Фикс: кэши mtw/fitLabel/wrapText, maxPx в 8px-бакеты
   (сталь-ключи при зуме). `522038a`
3. **Блюр-фон перерисовывал весь граф каждый кадр** — transform сидел в bgStamp:
   любой пан/зум под фокусом = полный muted-проход (все ноды+рёбра, полурез) +
   re-raster blur(5px). Фикс: transform вне штампа, delta-blit, settle-растр. `522038a`
4. **Еджи по одному** — ~1 280 stroke/кадр (beginPath+stroke на каждое ребро).
   Фикс: Path2D-бакеты по стилю, один stroke на стиль. `522038a`
5. **Невидимые дески считались** — descRamp>0 задолго до видимости: полный
   wrap+draw при альфе ≈0. Фикс: гейт descT≤0.06 → ноль работы. `522038a`
6. **Тело карточки растеризовалось каждый кадр** — 2-4 fillText + НОВЫЙ
   createLinearGradient на карточку на кадр. Фикс: offscreen-спрайт по контенту
   (LRU 400), кадр = 1 drawImage, descT = альфа+вертикальный рост. `5c04f3a`
7. **Ego-соседи с голым desc** (в v2.10.0) — суп; соседи теперь plate-чипы. `ddca778`

## Остаток (ранжировано; текущий пол ~8.5мс JS на карточном зуме)

1. **Кружки нод по одному** — ~490 × (beginPath+arc+fill+stroke) на кадр, на всех
   зумах. Самый большой оставшийся объём вызовов. Фикс: Path2D-бакеты по
   (цвет, альфа, радиус-бакет) как у еджей; stroke-обводку кластера — в тот же бакет.
   Ожидание: и JS, и растр (меньше стейт-свитчей Skia). ~30-60 строк.
2. **strokeText-гало лейблов** — 3-4px обводка текста = самый дорогой растровый
   примитив на строку; на живом GPU при dpr≈2 это заметно (хедлес не показывает).
   Фикс: лейбл-спрайты тем же приёмом, что desc (контент статичен: текст+цвет+фонт).
   Кэш ~сотни маленьких канвасов, LRU.
3. **Коллизия лейблов O(n²)** — `boxes.some(...)` на кандидата; при сотнях кандидатов
   = десятки тысяч rect-тестов на кадр. Пока терпимо (в бюджете), вырастет корпус —
   spatial hash (grid 64px). Мерить до стройки.
4. **Плотность on-screen** — цикл по всем нодам на кадр (screen-coords + счёт).
   O(n), дёшево, НЕ трогать (Bellard: не чинить то, что не болит).
5. **Минимап** — не всплыл в замерах; проверить отдельно только если юзер увидит.
6. **Worker/OffscreenCanvas** — НЕ надо. Пол 8.5мс JS + лёгкий растр покрывает
   60-120fps без архитектурных подвигов.

## Методика (грабли, чтоб не перемерять заново)

- Хедлес-vsync медианы 16.7/33.3мс = квантование кадров, не сигнал. Честно:
  (а) объёмы канвас-вызовов prototype-патчем, (б) тайминг rAF-колбэка initScript'ом
  (ставить ДО goto), (в) A/B со старым билдом: `git show HEAD~N:file > /tmp/...`.
- Колесо ≠ K: 9×240 колеса от fit дали K=0.86 (fit≈0.1). K читать
  `d3.zoomTransform(canvas).k`, зумить до цели циклом.
- Пан Playwright может жрать оверлей (панель ноды в центре экрана) —
  `document.elementFromPoint` проверять до замера.
- A/B-референс старого билда: `~/.cache/atlas/vault-old-2101.html` (v2.10.1).

## Состояние веток/пушей

- Коммиты арча: `ddca778` (2.10.1) → `522038a` (2.10.2) → `5c04f3a` (2.10.3) →
  `2555568` (2.10.4) + этот файл. Оба клона (`~/memory-atlas`, `~/src/memory-atlas`)
  + локальный bare синхронны. **GH pub + mac — НЕ пушено, триггер юзера.**
- Свежий вульт: `~/.cache/atlas/vault.html` (v2.10.4, пересобран).

---

# ADDENDUM 2026-07-10 (вечер) — v2.10.5: фреймтайм-проход по профилю

Продолжение утреннего аудита. Стенд повышен до **честного фреймтайма** и вшит в репо
(`perf/`): rAF-обёртка (js) + реальная rAF-дельта при разлоченном лимитере (dt = JS+растр+композит),
GPU-растр в headless живой (`--use-angle=gl-egl` → radeonsi, бенч печатает рендерер),
матрица вьюпорт × dpr × **масштаб текста (A−/A+)** × зум-K, разброс гасится
повторами (perf/focus-cell.mjs). A/B на одинаковых данных: perf/splice.py.

## Главная находка: цена была НЕ в канвас-вызовах

CPU-профиль пана на карточном зуме (perf/profile-pan.mjs):
**~55% JS — строковые ключи text-metrics кэшей**: wrapText 16.5% + tileMetrics 11.4%
+ mtw 10.8% + fitLabel 8.7% + bodySnip/cardText 7.6%. Кэши v2.10.2 сделали ПЕРЕСЧЁТ
дешёвым, но ключ (`g.font + maxPx + <полное тело ~400 символов>`) строился и хэшировался
на КАЖДУЮ ноду КАЖДЫЙ кадр — «дески тянут одеяло», акт II: тела перестали перемалываться,
но продолжали конкатенироваться.

## Что сделано (v2.10.5)

1. **Пер-нодовые мемо текст-лейаута** — tileMetrics и лейбл-пасс держат результат НА ноде
   под числовым штампом (8px-бакет ширины · число строк · TS · плита); горячий путь не
   трогает длинных строк вообще.
2. **Переиспользование объекта метрик** — ~500 свежих объектов/кадр из tileMetrics шли в GC;
   теперь один объект на ноду, мутируется (консюмеры живут один кадр — проверено).
3. **Спрайт точек миникарты** — точки движутся только при тёплой симуляции; пан/зум = блит
   + один strokeRect вместо ~1470 fillStyle+fillRect.
4. **Кэш DOM-элементов горячих ручек** — mode()/sizeMode() дёргали getElementById через
   nodeColor()/radius() ~2×1470 раз/кадр.

## Отвергнуто замером (анти-карго, НЕ пересобирать без числа)

- **Path2D-бакетинг кружков** (остаток №1 утреннего аудита) — построен, замерен, **откачен**:
  вызовы 1450→120/кадр, JS не сдвинулся (native-диспатч на GPU-канвасе не был ценой),
  а запись ~120 Path2D/кадр ДОБАВИЛА ~2ms на обзоре (9.3→11.5, 5 повторов, смежный A/B).
  Отметка оставлена комментом в шаблоне на месте петли нод.
- **Лейбл-спрайты** (остаток №2) — не строились: fillText ~100/кадр и strokeText ~10/кадр
  в профиле не доминанта; после мемо-фиксов подавно.

## Единая ось «масштаб» (разрешение · окно · текст)

Бюджет данных (лейблы И карточки — один lblBudget) нормирован на **площадь окна / TS²**,
якорь = 1920×1080 @ TS 1.25 (там поведение идентично v2.10.4), клампы [0.35, 2.2]:
маленькое окно / крупный текст больше не переполняются, большой монитор не пустует.
Проверка в матрице: fillText/кадр на 1280×800 ≈ 0.55× от 1920×1080 (было 1.0×).

## Числа (quick-матрица dpr2, смежные прогоны, медианы пана; js/dt ms)

| клетка | v2.10.4 js/dt | v2.10.5 js/dt |
|---|---|---|
| 1920 ts1.25 k1.0  | 12.4 / 15.0 | **6.4 / 10.0** |
| 1920 ts1.25 k1.56 | 13.1 / 14.7 | 13.1 / 16.3 (шов кроссфейда, см. ниже) |
| 1920 ts1.25 k2.3  | 15.8 / 16.5 | **9.4 / 13.0** |
| 1920 ts1.25 k3.25 | 15.1 / 15.8 | **8.8 / 12.5** |
| 1280 ts1.25 k1.0…3.25 | 6.6–10.7 | **4.1–5.9** |
| 1280 ts2 k2.3/k3.25 | 11 / 11.1 | **5.2 / 4.9** |

Полная матрица (72 клетки, dpr1+dpr2, TS 0.8/1.25/2.0): /tmp/fb-v2105-full.json.
**Живая конфигурация (1920×1080 dpr1): js 4–6ms, dt 5–8ms по ВСЕМ зумам и TS —
запас на 120fps есть.** Худшее место всей матрицы — синтетика 2560×1440@dpr2
(5K-растр): dt 19–34ms, растро-bound. Шов кроссфейда k≈1.5–1.6 — самая тяжёлая
зона зум-рампы (рисуются оба слоя: кружки+лейблы И карточки; бюджет там максимален) —
кандидат №1 следующего прохода, если шов почувствуется живьём.

## Вспышка фона в эго (live-репорт юзера этой же сессией)

«Светлое пятно мигает сквозь эго при драге»: виньетка (светлый центр
rgba(255,255,255,0.016)) — ЭКРАННАЯ, но была запечена в блюр-кэш, который при драге
блитится с дельтой камеры → пятно ездило со сдвигом и снапалось назад каждый BG_MS
ре-растр. Фикс: виньетка исключена из muted/кэша, рисуется экранно-фиксированной
поверх блита (drawVignette) — v2.10.5.

## Кнопки (той же сессией)

`#ofit` («✦ кадр») и `#langBtn` (RU↔EN) были белыми И мёртвыми по одной причине:
рукописный список id в CSS-правиле бара их не включал → ни стиля (UA-белый),
ни `pointer-events:auto` (унаследовали none от #bar) — клик проваливался в канвас
(Playwright: «canvas intercepts pointer events»). Правило заменено на `#bar button`
(один стиль на ВСЕ кнопки бара — и высота ряда перестала плавать), langBtn
растворяется через 10s простоя (opacity→0 + visibility, будится движением мыши).
Логика ofit (изометрия-ориентация) была целой — вернулась бесплатно.
