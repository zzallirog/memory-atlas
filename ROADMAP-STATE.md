# memory-atlas → editor: BUILD STATE (anti-ghost snapshot)

Живой трекер прогресса по EDITOR-ROADMAP.md. Обновляется по мере стройки, чтобы
через часы итераций **ничего не потерялось недостроенным**. Сорс правды по фичам =
`EDITOR-ROADMAP.md`; здесь — статус + порядок + что доказано.

Снапшот-база (verified, known-good): git tag `atlas-verified-20260717` @ `ad5de10`.

---

## ✅ DONE + VERIFIED (commit ad5de10)

- **`/api/create` + `Store.create`** (atlas-serve) — новая нота с нуля: зона=подпапка
  (сущ. или новая), slug, теги→frontmatter (форма импорта), path-guard, дедуп.
  Замер: **37/37 E2E** живьём.
- **Меню «＋ нова нота»** (browse) — имя/зона(datalist+новая)/теги/тіло → /api/create →
  regen+reload. EDIT-gated, i18n RU/EN/UK. Замер: **живой CDP-клик на Арче** — файл лёг
  в новую зону, граф 4→5.
- **`wrapSel` фикс** — восстановление выделения (`e`→`s`) после B/I/`[[]]`.
- Перепроверено за прошлой сессией: import **1900/1900** (живой прогон), serve **18/18**,
  self-test **48/48**.

Развёрнуто на Арче: `atlas-live/` (8137, demo-v2) обновлён этим кодом → refresh тестирует.

---

## 🔨 BUILD ORDER (утверждён юзером 2026-07-17)

- [x] **#1 bookmark → извлечение концепта** ⭐killer. ✅ **DONE + live-verified** (commit ниже):
      выделил текст в теле ноты → «＋ note from selection» → НОВАЯ нота (зона источника) +
      двусторонний линк `[[slug|title]]`. Замер CDP на Арче: 4→5 nodes, **0 ghosts**, бэклинк
      резолвится. Побочно найден+фикс: node id = norm(slug) '-'→'_' — линки/reselect теперь по
      slug-алиасу (был ghost + промах reselect и в меню). Закладка (a) отложена (persist-вопрос).
- [x] **#3 `[[` живой автокомплит** ✅ **DONE + live-verified** (Obsidian-паритет). Печать `[[`
      в редакторе → флоат-список заголовков **У КАРЕТКИ** (mirror-div) → стрелки/Enter/Tab/клик
      вставляют `[[Заголовок]]` (алиас для multi-word). Замер CDP: поп-ап рендерится у каретки,
      фаззи-матч, вставка верна (скрин `wlac.png`).
- [x] **#2 suggest → accept** ✅ **DONE + live-verified** (было СУПЕР-ВАЖНО — ДОСТРОЕНО).
      Замер CDP на Арче: панель «CONNECTIONS ATLAS INFERRED» на ноте показала **semantic-соседа**
      (badge ≈, Мишин «по смыслу»), ✓ → `[[фройд]]` записан в афект.md на диск, сосед выпал; «link all»
      рендерится. Semantic edges живы (arch Ollama). Баги пути: D3 мутирует `edge.source`→объект
      (eid-фикс); browse `D.edges`=только wiki → читать `DATA.edges` (граф несёт все kind).
      Multilingual `ATLAS_EMB_MODEL=embeddinggemma` = рычаг «лучше EN» (mac Ollama висел — не догнал).
      Atlas предлагает выведенные линки/теги/когорты как принимаемые кнопкой; клик пишет на диск.
      Уникальная ценность atlas; единственный путь связать нуль-линк корпус Миши. Тяжёлый.
      Если сессия рвётся — ДОДЕЛАТЬ приоритетом.
      **Обогащено интелом Миши (2026-07-17) — планка = не хуже Obsidian, лучше:**
      - (a) **Related-панель в превью** («connection» Миши): открыл ноту → СПРАВА близкие по смыслу
        статьи (semantic-соседи, что генератор уже считает) + читать тут же. Его killer daily-use.
      - (b) **Accept-to-link**: у каждой предложенной кнопка ✓ → пишет `[[сосед]]` на диск.
      - (c) **Auto-link-all** («просто лінкуй все підряд»): пакетно принять топ-N соседей для ноты/
        всего вульта. Миша НЕ курит вручную — «задаю тему, ші робить».
      - (d) **Multilingual-embed рычаг** («лучше»): его боль = EN-модель артефачит на украинском.
        Наш `$ATLAS_EMB_MODEL` → embeddinggemma (мультиязычная, Mac loopback) вместо nomic-EN.

## Интел Миши (для #2, не потерять)
Словник = 1 хаб-нод + 2300 нод + sources-ноды, СКРЫТЫ фильтром (карту не засорять). «connection» =
справа близкие по смыслу статьи, читаемые на месте. Семантик-поиск 50/50, EN-модель артефачит →
мультиязычная = «магія». Хочет авто-линк, не ручную возню. «схожі слова тяжіють до родичів».

## ⏸ DEFERRED
- **#4 GUI-импорт** («+ док» drag-drop → atlas-import) — дальний ящик (юзер). Расширяет
  /api/create → /api/import когда вернёмся.

## 🖐 LATER / MANUAL
- **#7 теги с весом** — тег несёт важность, тяжело-тегнутый концепт тянет родню в
  раскладке. Тяжёлый но важный; делать в **ручном режиме** (не автономно).

## открыто (мелочь)
- дискаверабилити зоны — закрыто datalist'ом (#5-adjacent).
- reload теряет камеру графа — паллиатив до #5 hot-swap.
- bookmark-персист: где хранить (frontmatter / sidecar-json / inline-маркер) — решить в #1.
