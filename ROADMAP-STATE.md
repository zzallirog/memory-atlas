# memory-atlas → editor: BUILD STATE (anti-ghost snapshot)

Живой трекер прогресса по EDITOR-ROADMAP.md. Обновляется по мере стройки, чтобы
через часы итераций **ничего не потерялось недостроенным**. Сорс правды по фичам =
`EDITOR-ROADMAP.md`; здесь — статус + порядок + что доказано.

Снапшот-база (verified, known-good): git tag `atlas-verified-20260717` @ `ad5de10`.

---

## ✅ DONE + VERIFIED (commit ad5de10)

- **`/api/create` + `Store.create`** (atlas-serve) — новая нота с нуля: зона=подпапка
  (сущ. или новая), slug, теги→frontmatter (форма импорта), path-guard, дедуп.
  Замер: **37/37 E2E** живьём.
- **Меню «＋ нова нота»** (browse) — имя/зона(datalist+новая)/теги/тіло → /api/create →
  regen+reload. EDIT-gated, i18n RU/EN/UK. Замер: **живой CDP-клик на Арче** — файл лёг
  в новую зону, граф 4→5.
- **`wrapSel` фикс** — восстановление выделения (`e`→`s`) после B/I/`[[]]`.
- Перепроверено за прошлой сессией: import **1900/1900** (живой прогон), serve **18/18**,
  self-test **48/48**.

Развёрнуто на Арче: `atlas-live/` (8137, demo-v2) обновлён этим кодом → refresh тестирует.

---

## 🔨 BUILD ORDER (утверждён юзером 2026-07-17)

- [x] **#1 bookmark → извлечение концепта** ⭐killer. ✅ **DONE + live-verified** (commit ниже):
      выделил текст в теле ноты → «＋ note from selection» → НОВАЯ нота (зона источника) +
      двусторонний линк `[[slug|title]]`. Замер CDP на Арче: 4→5 nodes, **0 ghosts**, бэклинк
      резолвится. Побочно найден+фикс: node id = norm(slug) '-'→'_' — линки/reselect теперь по
      slug-алиасу (был ghost + промах reselect и в меню). Закладка (a) отложена (persist-вопрос).
- [x] **#3 `[[` живой автокомплит** ✅ **DONE + live-verified** (Obsidian-паритет). Печать `[[`
      в редакторе → флоат-список заголовков **У КАРЕТКИ** (mirror-div) → стрелки/Enter/Tab/клик
      вставляют `[[Заголовок]]` (алиас для multi-word). Замер CDP: поп-ап рендерится у каретки,
      фаззи-матч, вставка верна (скрин `wlac.png`).
- [x] **#2 suggest → accept** ✅ **DONE + live-verified** (было СУПЕР-ВАЖНО — ДОСТРОЕНО).
      Замер CDP на Арче: панель «CONNECTIONS ATLAS INFERRED» на ноте показала **semantic-соседа**
      (badge ≈, Мишин «по смыслу»), ✓ → `[[фройд]]` записан в афект.md на диск, сосед выпал; «link all»
      рендерится. Semantic edges живы (arch Ollama). Баги пути: D3 мутирует `edge.source`→объект
      (eid-фикс); browse `D.edges`=только wiki → читать `DATA.edges` (граф несёт все kind).
      Multilingual `ATLAS_EMB_MODEL=embeddinggemma` = рычаг «лучше EN» (mac Ollama висел — не догнал).
      Atlas предлагает выведенные линки/теги/когорты как принимаемые кнопкой; клик пишет на диск.
      Уникальная ценность atlas; единственный путь связать нуль-линк корпус Миши. Тяжёлый.
      Если сессия рвётся — ДОДЕЛАТЬ приоритетом.
      **Обогащено интелом Миши (2026-07-17) — планка = не хуже Obsidian, лучше:**
      - (a) **Related-панель в превью** («connection» Миши): открыл ноту → СПРАВА близкие по смыслу
        статьи (semantic-соседи, что генератор уже считает) + читать тут же. Его killer daily-use.
      - (b) **Accept-to-link**: у каждой предложенной кнопка ✓ → пишет `[[сосед]]` на диск.
      - (c) **Auto-link-all** («просто лінкуй все підряд»): пакетно принять топ-N соседей для ноты/
        всего вульта. Миша НЕ курит вручную — «задаю тему, ші робить».
      - (d) **Multilingual-embed рычаг** («лучше»): его боль = EN-модель артефачит на украинском.
        Наш `$ATLAS_EMB_MODEL` → embeddinggemma (мультиязычная, Mac loopback) вместо nomic-EN.

## Интел Миши (для #2, не потерять)
Словник = 1 хаб-нод + 2300 нод + sources-ноды, СКРЫТЫ фильтром (карту не засорять). «connection» =
справа близкие по смыслу статьи, читаемые на месте. Семантик-поиск 50/50, EN-модель артефачит →
мультиязычная = «магія». Хочет авто-линк, не ручную возню. «схожі слова тяжіють до родичів».

## ⏸ DEFERRED
- **#4 GUI-импорт** («+ док» drag-drop → atlas-import) — дальний ящик (юзер). Расширяет
  /api/create → /api/import когда вернёмся.

## 🖐 LATER / MANUAL
- **#7 теги с весом** — тег несёт важность, тяжело-тегнутый концепт тянет родню в
  раскладке. Тяжёлый но важный; делать в **ручном режиме** (не автономно).

## открыто (мелочь)
- дискаверабилити зоны — закрыто datalist'ом (#5-adjacent).
- reload теряет камеру графа — паллиатив до #5 hot-swap.
- bookmark-персист: где хранить (frontmatter / sidecar-json / inline-маркер) — решить в #1.

---

## 🆕 #8 — Мишина ДВУХУРОВНЕВАЯ КАРТА (его чертёж, 2026-07-17, НЕ ПОТЕРЯТЬ)

Пришёл полный пейст Миши. Свежак, в старом «Интел Миши» его НЕТ — снапшот тут, чтоб не сгинул.
Не глубина-под-воду, не drill-down — **ДВА РАЗНЫХ ГРАФА над одними нодами, разведённые по
семантике рёбер**. Керн, который прошлые чтения проебали: **типизация нод**.

**Тезис (его слова):** Obsidian смешивает membership и context в один граф → при масштабе = каша.
Разведи их в две карты → чисто. «схожі слова тяжіють до родичів» — норма, не баг.

**Типизация нод (А/В/С, до 4 уровней):**
- **Тип А** (высший, центрирующий) = предмет (общая психология, возрастная, гендерная).
- **Тип В** (распределяющий) = темы предмета (мышление, память…).
- **Тип С** (подтемы, деталь В) = типы/особенности/разновидности.
- 4-й уровень — для особых нужд, редко >4.

**Карта 1 — чистый радиальный скелет (для человека):** А в центре → орбита В → у В орбита С.
Рёбра ТОЛЬКО membership (родитель→ребёнок). Один А = ноль артефактов.

**Карта 2 — контекст-паутина (под капотом):** рёбра по содержанию статей, пересекают группы
СВОБОДНО. Его наблюдение (ключ): нода С может иметь БОЛЬШЕ рёбер с чужим В — **и это нормально,
прописку не меняет**. membership ≠ context-degree. ← вот что Obsidian конфлейтит.

**Развязка = смерть каши:** новый А на карте1 линкуется только с А; на карте2 — с чем угодно.

**UX:** тычешь карту1 → наводишь ноду → правое окно (**мультивикно**) даёт её карту2 +
градация коэффициентов связей. Right-panel идея — от того Obsidian-плагина.

**Статус в коде:**
- карта2-данные ЕСТЬ (edge kind=semantic/tag_overlap/temporal/homology) + панель #2 (список).
- карта1 НЕТ: у нод только плоская `zone` + type=memory/ghost, нет tier А/В/С, нет радиал-layout.
- Раскладка = самописный canvas-force (cell-grid), не d3.force.

**Развилка масштаба (откуда берутся типы — flat 2300 глоссарий Миши иерархии НЕ несёт):**
- (a) **folder-depth** — tier = глубина подпапки. Бесплатно, если Миша вложит вульт. MVP-путь.
- (b) **auto-infer** — LLM классифицирует 2300 терминов в А/В/С. Тяжело = отдельный воркфлоу (3 опуса).
- (c) **ручная курация** — UI назначает tier кликом. Средне.

**План (эмердж, натив, оба-вида+тумблер):**
- MVP (доказать механику, Bellard): tier-поле + карта1 радиал-layout + нативный тумблер
  GRAPH→[taxonomy‖context‖overlay], на demo/folder-depth. Доказать «каша→чисто».
- Follow-on: типизация 2300 (b/c) — воркфлоу, НЕ этот проход.
- Z-глубина юзера = карта2 как сменный слой → всплывёт в 3D позже. Строить карту2 слоем, не хардкодом.

### ✅ MVP DONE + VERIFIED (2026-07-17, эта сессия)
Керн Миши построен НАТИВНО — переиспользует dendro-layout + groupMode-ось, не новая физика (Bellard).
- **Генератор:** эмитит membership-рёбра `kind="part_of"` из `node.part_of` (карта1-скелет).
  Замер на вложенном psy-вульте (8 нот А/В/С): **6 part_of рёбер, иерархия C→B→A верна**
  (Абстрактне→Мислення→Загальна…). Cross-group context доказан: `Вікова криза`(дитя Вікової)
  контекстно линкует `Абстрактне мислення`(дитя Загальної) через wiki — **прописку НЕ меняет**
  (Мишин кейс «С тяжелеет к чужому В» — работает на уровне данных).
- **Рантайм (template):** `tier` как ось `groupVal`/`groupKey` + `<option>`; пресет **`taxonomy`**
  (layout=dendro, group=tier, kinds=[part_of]); `applyPreset` чтит `p.group`. i18n RU/EN/UK сведён.
- **Тумблер = нативный taskPreset** (карта1=«таксономія», обратно=любой др. вид). Карта2 =
  существующие context-рёбра (semantic/wiki) + related-панель #2.
- **Замеры:** self-test **48/48**; headless-Chrome CDP рендер карта1 = **чистое радиальное дерево**
  8/8 нод читаемы (скрин `scratchpad/shot_taxonomy.png`) vs force = комок с налезающими лейблами.
- **Развилка масштаба ОТКРЫТА:** tier для живых 2300 flat = follow-on (auto-infer воркфлоу / курация).
  MVP взял tier из frontmatter (`tier:`/`part_of:` уже парсились генератором — латентные поля).
- Патч-скрипты + бэкапы: `scratchpad/patch_*.py`, `*.template.html.bak-*`.

---

## 🆕 #9 — ИНТЕРАКШН-МОДЕЛЬ (спека юзера, 2026-07-18, поток — НЕ реализовывать слепо)

Юзер проектирует вслух после того как пин-тост подтвердил rmb. Стоп-вердикт (Core 2): это
интеракшн-РЕДИЗАЙН под **«два типа юзера»** (= корневой «різний рівень задуму» Миши), не баг-пила.
Снапшот, чтоб не сгинул. Реализация — слайсами по локнутой спеке, спайн первым.

**Диагностика (закрыта):** rmb ПИНИТ (тост 📌 подтвердил). Юзер полюбил rmb-поведение =
**pin двигает камеру-контекст БЕЗ смены масштаба** («context delivery» контракт v1.1). lmb =
показывает связи + двигает камеру (если cam≠stay).

**Два типа юзера (спайн модели):**
- **левокликеры-превьюшеры** — lmb = превью в правом баре (полный текст, заметка, связи).
- **ховер-пиннеры** — наводят + пинят лёгкие превью-карточки прямо на графе.

**Ремап кнопок (предложение юзера):**
- **LMB** = превью/связи + камера (+ возможность СКРЫТЬ, фейд ускоряющийся→замедляющийся).
- **RMB** = ДВИЖЕНИЕ (перестановка ноды без смены камеры — то что полюбил).
- **MMB** = пин непосредственно.
- **RMB 2-й раз на той же ноде** = создать **ховер-пин** (анимация + лёгкая прозрачность
  в завис. от масштаба — сколько % ноды видно — И от того, открыт ли правый сайд).

**Два артефакта пина (НЕ должны коллизиться):**
- **node-text-preview-pin** → верхний ЛЕВЫЙ угол, стек.
- **hover-preview-pin** → верхний ПРАВЫЙ угол, стек.
- ховер-превью = **мини-правый-бар нативно** (неогр. текст + заметка + связи, то же что lmb в баре).

**Правила feel:** прозрачность/контраст ∝ масштаб (% видимости ноды) + состояние правого сайда;
анимации фейда с ease-in-out; ховер наследует силу магнита → плавный рост яркости.

**Floor:** 10 взаимозависимых анимированных поведений слепым патчем = каша + неверифицируемо на
`file://`. План: (1) локнуть спеку, (2) спайн-слайс = ремап кнопок + разделение двух пин-артефактов,
(3) полишь (анимации/прозрачность-по-зуму/стек-углы) слайсами, каждый → deploy → swappy юзера.
Развилка спайна: подтвердить button-map + углы стека ДО кода.

---

## 🆕 Сессия 2026-07-18 (Fable, хендоф HANDOFF-COLD-2026-07-18 → 8 фаз, оси юзера: ida-box фейды · разгрузка кнопок · Мишина модель)

Все правки — python-патчеры (`scratchpad/patch_*.py` сессии 578c27d4) + `.bak-*`; self-test 48→**50**;
кнопка-проб харнес `probe_buttons.py` (инвентарь `<button>`: visibility/pointer-events) —
бейзлайн vs финал: **0 потерянных кнопок**.

- **ak-кит** (`e83713c`): ida-box фейд-словарь на граф-стороне — idaFieldIn-каскад #panelBody
  (stagger nth-child, фирит ТОЛЬКО на смену ноды — паттерн animPass), sheets-каскад,
  reduced-motion гейт. ⚠ headless-верифай: macOS Reduce Motion ТЕЧЁТ в headless Chrome —
  render_atlas.py теперь эмулирует no-preference (Emulation.setEmulatedMedia).
- **задача 10** (`2fa062a`, `+fix .abtn scope`): hover-pin = дизайн мышь-тултипа — расшарен CSS
  `#tooltip .tmeta/.tdesc/.ttags/.tlinks` на `.hpcard`, тег-чипы цветные, спавн/деспавн ida-фейдом.
- **задача 1** (`9acbcaa`): reader-mode — select = левый HUD уезжает фейдом, `#panel` растёт
  330→520px через `--panelw` (миникарта+hover-стек следуют), top=64 (не наезжает на viewtabs);
  выход deselect/esc. Авто (юзер: «автоматом»).
- **задачи 2+3+7** (`aba8fce`): прозрачность hover-стека ∝ зум (`--hpz`) + dim при открытой панели
  (hover будит); драг-релиз = снап в ближайший угол (правая колонка объезжает панель);
  hide-ноды = 380ms ease-out фейд на канвасе (hideFade map, visible() держит на время фейда).
- **задачи 6+8** (`f4fa08d`): бар дремлет на idle 10s (langBtn-таймер расшарен, stats живой);
  HUD: секции после «вид» + карты zones/kinds/tags/legend/views = `.pw` за «⚙ ещё…» (persist
  `CFG.hudFold`, свёрнуто по дефолту — Мишин минимум: поиск+вид); кнопка «📍 hover» в панели.
- **задача 5** (`97a5aff`): «категория · авто» — единая ось zone/cluster: CAT_AXIS = zone при >1
  реальной зоне, иначе louvain (плоский корпус Миши получает структуру сам); первая опция
  color/group, персист сырым значением (round-trip).
- **задача 11** (`e818cb0`): browse context-меню — «◌ в граф» + «📌 pin» всегда, edit/＋нота
  под EDIT-гейтом; pin из browse через граф-scope togglePin.
- **задача 9** (`774f626`): `--fast-regen` = wiki+shared_ref+semantic, O(n²) tag/temporal и
  git-session выключены (авто-синк на 2300 нот). **+ UK-фикс** (user-ukraine-no-ru-defaults):
  `--lang uk` легален, локаль uk → uk (был uk→ru в ДВУХ местах: locale-auto + lang_token
  подстановки '__LANG__'); тесты test_fast_regen_skips_heavy_detectors + test_lang_token_uk_survives.

**Очередь хендофа:** закрыты 1,2,3,5,6,7,8,10,11 (9 из 11). Осталось: **4** (tier-инференс А/В/С
на 2300 — воркфлоу «3 опуса», юзер триггерит) и полишь по swappy-фидбеку.
